[section Rationale]

[heading No Derived-Type Contraints]

There is no constraint on the `Derived` template parameter to _iter_facade_,
because `Derived` is an incomplete type within _iter_facade_.

[endsect]
